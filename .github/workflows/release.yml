name: Public Release Disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-22.04
    steps:
      - name: Explain current policy
        run: |
          echo "bianma-app 当前不是正式发布仓。"
          echo "正式构建、release 和 updater 由私有仓 bianma-app-product 承接。"
          echo "如需恢复公开发布链路，必须先单独完成仓库边界治理。"
