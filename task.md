# bianma-app 公开仓维护说明

## 当前定位

- `bianma-app` 当前只承接公开可见内容。
- 默认范围包括：README、用户手册、公开中文文案、公开协作说明。
- 默认不承接：私有实现、私有发布链路、updater、内部任务记录、内部迁移设计。

## 当前状态

- ✅ 已完成（2026-04-12）：清理公开深链接测试页 `deplink.html` 的品牌口径，统一页面标题、说明文案与使用提示到 `bianma-app` / `bianma://` 主语境。
- ✅ 已完成（2026-04-12）：停用公开仓 `release.yml`，避免把公开仓误当成当前正式发布仓。

## 维护边界

- 仅处理公开文档与公开协作入口。
- 不处理 `src/`、`src-tauri/`、`package.json`、`tauri.conf.json`。
- 不处理私有产品层、私有发布层、私有运营层内容。
- 不在本仓记录内部任务拆解、灰度状态和私有交接事项。

## 仍待后续复查

- ⚠️ `CODE_OF_CONDUCT.md` 仍存在旧邮箱，默认只记录，不在本轮处理。
- ⚠️ 公开 README / 用户手册仍应持续做私有泄漏复查，尤其是下载、升级、深链接和兼容说明相关章节。

## 说明

- 本文件只保留公开仓维护边界，不再作为内部任务记录使用。
