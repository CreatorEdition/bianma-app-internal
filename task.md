# bianma-app 公开仓任务记录

## 当前状态

- ✅ 已完成（2026-04-12）：清理公开深链接测试页 `deplink.html` 的品牌口径，统一页面标题、说明文案与使用提示到 `bianma-app` / `bianma://` 主语境。

## 本轮范围

- 仅处理公开文档与公开协作入口。
- 不处理 `src/`、`src-tauri/`、`package.json`、`tauri.conf.json`。
- 不处理私有产品层、私有发布层、私有运营层内容。

## 候选清理项

- ⚠️ `CODE_OF_CONDUCT.md` 仍存在旧邮箱，默认只记录，不在本轮处理。
- ⚠️ `.github/workflows/release.yml` 仍存在旧 public naming / artifact naming，默认只记录，不在本轮处理。

## 说明

- 本文件用于补齐公开仓维护上下文，便于后续文档 / 规划 Agent 接手。
